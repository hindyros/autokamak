

[1] GitHub - OpenFUSIONToolkit/OpenFUSIONToolkit: The Open FUSION Toolkit (OFT) is a suite of modeling tools, and their underlying finite element framework, for problems in plasma and fusion research and engineering in arbitrary 2D and 3D geometries. · GitHub (https://github.com/OpenFUSIONToolkit/OpenFUSIONToolkit)

Summary:
The retrieved text is general project overview information about the **Open FUSION Toolkit (OFT)**, not the specific `gs_Domain` / `create_isoflux` / mesh-from-LCFS workflow you’re looking for.

### What it tells us (relevant to your task)
- **OFT purpose/capability:** A suite of plasma/fusion modeling tools built on a finite-element framework that supports **arbitrary 2D/3D geometries** and **variable-order FEM** on **unstructured meshes**:
  - 2D: triangular / quadrilateral
  - 3D: tetrahedral / hexahedral
- **Tools included:** Mentions that OFT includes **TokaMaker** (axisymmetric static/time-dependent ideal MHD equilibria) along with ThinCurr, MUG, Marklin. This indicates TokaMaker is the likely location of Grad–Shafranov (`gs_...`) domain examples, but the excerpt itself does not provide them.
- **Docs/examples location hint:** Documentation exists online and in binary packages at:
  - `/path/to/oft/install/doc/Documentation.html`
  This is the best actionable pointer in the excerpt for where `gs_Domain` and `create_isoflux` usage might be documented.
- **Installation note (python):** It briefly references a “python directory for your installation…” but the excerpt is incomplete and does not describe a **Python API** for `gs_Domain` specifically.

### What it does *not* provide
- No example code showing **`gs_Domain`** usage.
- No mention or examples of **`create_isoflux`**.
- No information about a **Python API binding** for `gs_Domain`.
- No method described for creating a **mesh from LCFS boundary points and `dx`**.

### Implication for next retrieval step
To find what you want, the excerpt suggests you should search within:
- The packaged HTML docs: `.../doc/Documentation.html`
- Any included examples or Python package directories in the installation, especially under TokaMaker / Grad–Shafranov sections.

----------------------------------------

[2] GitHub - defisym/OpenFusionExamples: Clickteam Fusion Open Source Stuffs · GitHub (https://github.com/defisym/OpenFusionExamples)

Summary:
The provided content is **not relevant** to the task.

- It describes a GitHub repository containing open-source resources for **Clickteam Fusion** (e.g., shaders, extensions, tutorial `.mfa` files) and instructions for contributing via forks/PRs.
- It contains **no mentions** of **OpenFUSIONToolkit**, **OFT**, **TokaMaker**, **gs_Domain**, **create_isoflux**, or any **Python API** related to `gs_Domain`.
- It does **not** provide any information about creating a mesh from **LCFS boundary points** and `dx`.

----------------------------------------

[3] GitHub - hansec/OpenFUSIONToolkit: The Open FUSION Toolkit (OFT) is a suite of modeling tools, and their underlying finite element framework, for problems in plasma and fusion research and engineering in arbitrary 2D and 3D geometries. (https://github.com/hansec/OpenFUSIONToolkit)

Summary:
The retrieved content is high-level project overview for the Open FUSION Toolkit (OFT) and does **not** contain the specific items you’re looking for (no `gs_Domain`, no `create_isoflux`, no Python API details, and no mesh-from-LCFS recipe). Key points it *does* provide:

- **What OFT is:** A suite of modeling tools plus a finite-element framework aimed at plasma/fusion problems in arbitrary **2D/3D geometries**.
- **Numerics/meshing capabilities:** Supports **variable-order finite elements** on **unstructured** grids:
  - Tetrahedral / triangular meshes
  - Hexahedral / quadrilateral meshes
- **Tool suite components:**
  - **TokaMaker**: axisymmetric static and time-dependent ideal MHD equilibria
  - **ThinCurr**: inductively excited thin-wall currents
  - **MUG**: time-dependent nonlinear/linear extended MHD
  - **Marklin**: 3D force-free ideal MHD equilibria
- **Project maturity:** Marked as **beta**, transitioning toward fully open-source with documentation/examples being completed toward v1.0.
- **Distribution/install info:**
  - Prebuilt binaries for Linux x86 and macOS (x86/arm64) available on GitHub releases
  - Documentation is included in binary packages and can be opened at:  
    `/path/to/oft/install/doc/Documentation.html`
- **Contribution/licensing:** Points to `CONTRIBUTING.md` and includes copyright notice.

Implication for your task: this snippet establishes that OFT has an FE framework and mesh support, but you’ll need to locate **examples/docs/source** elsewhere (wiki, Documentation.html, or repo examples) to find concrete usage of `gs_Domain`, `create_isoflux`, and any Python API for constructing a mesh from LCFS boundary points + `dx`.

----------------------------------------

[4] OpenFUSIONToolkit (https://github.com/OpenFUSIONToolkit)

Summary:
The retrieved content is only high-level GitHub/organization metadata; it does not include any actual code, examples, or API documentation relevant to **gs_Domain**, **create_isoflux**, or building a mesh from **LCFS boundary points + dx**.

What it *does* indicate (useful for where to look next):

- **Repositories to search for examples**
  - `OpenFUSIONToolkit/GPEC` (Julia; updated Mar 16, 2026): likely contains recent development and possibly example usage if GPEC integrates with OFT/TokaMaker workflows.
  - `OpenFUSIONToolkit/OpenFUSIONToolkit-Tutorials`: explicitly “Storage for tutorial and demonstrations for OFT” and is the most promising place to find *worked examples* of `gs_Domain` and `create_isoflux`, plus meshing workflows.
  - `OpenFUSIONToolkit/OpenFUSIONToolkit`: main toolkit repo; likely contains the authoritative implementation and any Python bindings/API for `gs_Domain` (if exposed).
  - `OpenFUSIONToolkit/.github`: org-level config; unlikely to contain technical examples.

- **No direct answer to the task requirements**
  - No snippet demonstrating `gs_Domain`.
  - No snippet demonstrating `create_isoflux`.
  - No evidence (from this text) of a Python API for `gs_Domain`.
  - No method shown for generating a mesh from LCFS boundary points and `dx`.

Net: this content helps identify *which repos* to inspect (Tutorials + main OFT), but provides **no concrete example code or API details** needed for your specific goals.

----------------------------------------

[5] Releases · OpenFUSIONToolkit/OpenFUSIONToolkit (https://github.com/openfusiontoolkit/OpenFUSIONToolkit/releases)

Summary:
### What this retrieved content is
The text is a **GitHub Releases / changelog excerpt** for multiple **OpenFUSIONToolkit (OFT)** beta releases (beta1 → beta7). It does **not** include code snippets, but it does contain several pointers to where *examples* and *API changes* related to your task likely live.

---

### Direct signals relevant to your task (gs_Domain / create_isoflux / meshing from LCFS)
1. **gs_Domain appears explicitly (Python API / breaking change)**
   - In the **v1.0.0-beta4** notes:  
     > “For models with poloidal field coils the coil definition dictionary (eg. from `gs_Domain.get_coils()`) must be passed to `TokaMaker.setup_regions()`.”
   - Implication for you:
     - There *is* a Python-side `gs_Domain` object with at least `get_coils()`.
     - There is a workflow involving `TokaMaker.setup_regions(...)` that now expects a **coil definition dictionary** sourced from `gs_Domain`.
     - This is a strong hint that **TokaMaker examples (HBT-EP meshing/equilibrium)** demonstrate `gs_Domain` usage patterns.

2. **Isoflux examples were added (most likely where `create_isoflux` is demonstrated)**
   - In the **v1.0.0-beta7** notes:
     - “**TokaMaker: Add Isoflux/NSTX examples** [#199]”
   - Implication:
     - If you need **`create_isoflux` usage**, the newly added **Isoflux/NSTX examples** are the most likely place to find it.
     - This changelog doesn’t show the function name `create_isoflux`, but it clearly flags that *Isoflux example content exists* starting beta7.

3. **Meshing backend changes that affect “mesh from LCFS boundary points + dx”**
   - **beta5**: “An internal copy of **triangle** has been added to OFT, so installation of the triangle python package is no longer required to generate **TokaMaker meshes**.”
   - **beta7**: “Add support for using **Cubit** as the meshing backend [#186]”
   - Implication:
     - OFT/TokaMaker mesh generation can be done internally (triangle) and/or via Cubit.
     - Your desired method “create mesh from LCFS boundary points and dx” is likely implemented in the “native”/internal meshing workflow used by TokaMaker examples (especially “HBT-EP meshing”).

4. **Interface refactors / environment initialization breaking changes (affects how you run examples)**
   - **beta7**: “breaking changes for the OFT Python interface as a initialization of the environment has changed… See … examples … for demonstrations.”
   - **beta6**: “breaking changes for ThinCurr models as the Python interface has changed significantly…”
   - **beta5**: “breaking changes for TokaMaker models as the Python package structure has changed…”
   - Implication:
     - When you search for `gs_Domain` or `create_isoflux` usage in examples, you must ensure you’re looking at examples matching your installed version because import paths / initialization patterns changed.

---

### Where this content points you to look next (for actual examples/snippets)
Based on the release notes, the best targets to retrieve code demonstrating your needs are:

- **TokaMaker Isoflux/NSTX examples** (added in **beta7**, PR #199): likely where “isoflux” target construction (possibly `create_isoflux`) is shown.
- **HBT-EP meshing and HBT-EP equilibrium examples** (called out in **beta4/beta5** notes): likely where `gs_Domain` is instantiated and where domain/boundary meshing from curves is done.
- **Documentation + examples** are repeatedly referenced as the authoritative source for the updated Python interface and initialization (especially beta7’s environment setup change).

---

### Key extracted facts you can use immediately
- `gs_Domain.get_coils()` exists and returns a **coil definition dictionary** intended to be passed into `TokaMaker.setup_regions()`. (beta4)
- Isoflux example content exists in-repo starting **v1.0.0-beta7** (“Isoflux/NSTX examples”). (beta7)
- OFT can generate TokaMaker meshes without external `triangle` Python dependency (internal triangle copy added). (beta5)
- Multiple releases include **breaking Python API changes**, so example code must match the release version you’re using. (beta5–beta7)

---

### What is *not* present in the retrieved text
- No explicit mention of `create_isoflux` by name.
- No explicit description of a `gs_Domain` constructor/API beyond `get_coils()`.
- No explicit recipe for “mesh from LCFS boundary points and dx” (only hints that meshing support exists and where to find demonstrations).

If you paste the actual **Isoflux/NSTX example** code or the **HBT-EP meshing** example content, I can extract the concrete `gs_Domain`/`create_isoflux` calls and the exact mesh-from-boundary workflow.