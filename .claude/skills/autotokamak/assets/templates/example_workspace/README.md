# <example-name>

One-sentence description of what this example does.

## Run

```bash
python run.py config.yaml
```

Outputs land under `outputs/<case_slug>_<hash>/`.

## Config

See `config.yaml`. Fields are validated by `autotokamak.core.schema.EquilibriumConfig`.

## What this example demonstrates

- <fill in>
- <fill in>

## What it does NOT do

- Not free-boundary — LCFS is prescribed.
- No custom pressure profiles — uses TokaMaker defaults.
- Not agent-driven — this workspace is runnable standalone.
